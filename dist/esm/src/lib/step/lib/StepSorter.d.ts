import Step from './Step.js';
/**
 * Sorts steps by their number
 * @param a The first step
 * @param b The second step
 */
declare const _default: (a: Step, b: Step) => number;
export default _default;
