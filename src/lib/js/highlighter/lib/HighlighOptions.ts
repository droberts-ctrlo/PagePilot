/**
 * Highlight options
 * @property {number} defaultX - Default x position
 * @property {number} defaultY - Default y position
 * @property {number} defaultWidth - Default width
 * @property {number} defaultHeight - Default height
 */
export default interface HighlightOptions {
    defaultX?: number
    defaultY?: number
    defaultWidth?: number
    defaultHeight?: number
}
